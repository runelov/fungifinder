{
  "name": "fungifinder-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply fungifinder --local",
    "db:migrate:remote": "wrangler d1 migrations apply fungifinder --remote"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
